<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eLearnPlayer.mapper.ListeningAudioInfoMapper">
  <resultMap id="BaseResultMap" type="com.eLearnPlayer.entity.ListeningAudioInfo">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="Audio_id" jdbcType="INTEGER" property="audioId" />
    <result column="Audio_title" jdbcType="VARCHAR" property="audioTitle" />
    <result column="Audio_file" jdbcType="VARCHAR" property="audioFile" />
    <result column="Level" jdbcType="INTEGER" property="level" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    Audio_id, Audio_title, Audio_file, Level
  </sql>
    <insert id="insertListeningAudio">
      INSERT INTO listening_audio_info (Audio_title,Audio_file,Level)
      VALUES (#{audioTitle},#{audioFile},#{level})
    </insert>
  <insert id="insertListeningQuestion">
    INSERT INTO listening_questions_info (Audio_id,Passage_text,Correct_answer)
    VALUES (#{audioId},#{passageText},#{correctAnswer})
  </insert>
  <insert id="insertListeningChoice">
    INSERT INTO listening_choices_info(Listening_Question_id,Choice_index,Choice_text)
    VALUES (#{listeningQuestionId},#{choiceIndex},#{choiceText})
  </insert>
  <select id="getAudioBySelectedRankAndLevel" resultType="com.eLearnPlayer.entity.ListeningAudioInfo">
    SELECT * FROM listening_audio_info
    WHERE Level = #{level}
        AND Audio_id = (SELECT Audio_id
            FROM (SELECT Audio_id FROM listening_audio_info WHERE Level = #{level} LIMIT #{selectedRank},1) AS tmp)
  </select>
  <select id="getQuestionsByAudioId" resultType="com.eLearnPlayer.entity.ListeningQuestionsInfo">
    SELECT * FROM listening_questions_info WHERE Audio_id = #{audioId}
  </select>
  <select id="getChoicesByQuestions" resultType="com.eLearnPlayer.entity.ListeningChoicesInfo">
    SELECT * FROM listening_choices_info WHERE listening_question_id IN
    <foreach item="question" collection="list" open="(" separator="," close=")">
      #{question.listeningQuestionId}
    </foreach>
  </select>
  <select id="getLevelInfoByLevel" resultType="com.eLearnPlayer.entity.LevelInfo">
    SELECT * FROM level_info WHERE level = #{level}
  </select>
  <select id="getALlListeningAudioInfo" resultType="com.eLearnPlayer.entity.ListeningAudioInfo">
    SELECT * FROM listening_audio_info
  </select>
  <select id="getAudioByAudioId" resultType="com.eLearnPlayer.entity.ListeningAudioInfo">
    SELECT * FROM listening_audio_info WHERE Audio_id = #{audioId}
  </select>
  <select id="getAudioIdByAudioTitle" resultType="java.lang.Integer">
    SELECT Audio_id
    FROM listening_audio_info
    WHERE Audio_title = #{audioTitle}
  </select>
  <select id="getListeningQuestionIdByPassageText" resultType="java.lang.Integer">
    SELECT Listening_Question_id
    FROM listening_questions_info
    WHERE Passage_text = #{passageText}
      AND Audio_id = #{audioId}
      AND Correct_answer = #{correctAnswer}
  </select>

</mapper>